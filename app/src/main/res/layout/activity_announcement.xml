<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.AnnouncementActivity"
    android:background="@color/color2">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingTop="8dp"
        android:paddingHorizontal="8dp"
        android:background="@color/color2">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar_Announcement"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:background="@drawable/background_oval_sec_top"
            android:elevation="4dp"
            app:title=" "
            app:titleTextColor="#DEDEDE"
            >

            <!-- Geri düğmesi -->
            <ImageButton
                android:id="@+id/toolbarBackButton_Announcement"
                android:layout_width="55dp"
                android:layout_height="55dp"
                android:background="@null"
                android:contentDescription="Back"
                android:layout_alignParentStart="true"
                app:srcCompat="@drawable/arrow_back" />
            <TextView
                android:id="@+id/toolbarTitle_Announcement"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_marginStart="30dp"
                android:text="Duyurular"
                android:textColor="#DEDEDE"
                android:textSize="19sp"
                android:textStyle="bold"/>
        </androidx.appcompat.widget.Toolbar>



        <ListView
            android:id="@+id/announcementListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/primary"/>

    </LinearLayout>

</RelativeLayout>